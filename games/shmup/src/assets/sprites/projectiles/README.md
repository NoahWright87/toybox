# assets/sprites/projectiles/

Bullets, missiles, beams. Naming: `projectile_{name}_{frame}.png` (e.g.
`projectile_bullet_player_01.png`). Default canvas: 32×32 — see
`../../../sprites/README.md` for the full convention guide.

No art has been generated yet — every `projectiles` sprite renders as its
manifest-declared colored placeholder primitive until a PNG lands here.
