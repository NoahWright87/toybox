# assets/sprites/ships/

Player and allied craft. Naming: `ship_{name}_{state}_{frame}.png`
(e.g. `ship_player_idle_01.png`). Default canvas: 64×64 — see
`../../../sprites/README.md` for the full convention guide.

No art has been generated yet — every `ships` sprite renders as its
manifest-declared colored placeholder primitive until a PNG lands here.
